# These files automatically update when edited/saved in vim:

# keys	filename				description
cfbf		${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-files	# This file, a list of bookmarked files
cfbd		${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-dirs		# A list of bookmarked directories similar to this file
cfxr	${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources	# Colors, themes and variables for X11
cfdb	~/.local/src/dwmblocks/config.h				# dwmblocks: the status bar for dwm


# These do not update automatically, but on the next new instance of a program:

cfv		${XDG_CONFIG_HOME:-$HOME/.config}/nvim/init.vim		# vim/neovim config
cfz		$ZDOTDIR/.zshrc						# zsh (shell) config
cfa		${XDG_CONFIG_HOME:-$HOME/.config}/shell/aliasrc		# aliases used by zsh (and potentially other shells)
cfp		${XDG_CONFIG_HOME:-$HOME/.config}/shell/profile		# profile file for login settings for zsh
cfmu	${XDG_CONFIG_HOME:-$HOME/.config}/mutt/muttrc		# mutt (email client) config
cfnbc	${XDG_CONFIG_HOME:-$HOME/.config}/newsboat/config	# newsboat (RSS reader)
cfnbu	${XDG_CONFIG_HOME:-$HOME/.config}/newsboat/urls		# RSS urls for newsboat
cfncb	${XDG_CONFIG_HOME:-$HOME/.config}/ncmpcpp/bindings	# ncmpcpp (music player) keybinds file
cfncc	${XDG_CONFIG_HOME:-$HOME/.config}/ncmpcpp/config	# ncmpcpp (music player) config
cflf	${XDG_CONFIG_HOME:-$HOME/.config}/lf/lfrc		# lf (file browser) config
cfls	${XDG_CONFIG_HOME:-$HOME/.config}/lf/scope		# lf's scope/preview file
cfsk	${XDG_CONFIG_HOME:-$HOME/.config}/sxiv/exec/key-handler	# sxiv (image viewer) key/script handler
